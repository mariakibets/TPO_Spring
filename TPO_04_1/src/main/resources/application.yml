spring:
  h2:
    console:
      enabled: true
      path: /my-h2-console
  datasource:
    url: jdbc:h2:mem:books
    username: s30855
    password: test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      data-locations: classpath:sql-data/*.sql
